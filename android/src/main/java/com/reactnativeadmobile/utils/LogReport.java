package com.reactnativeadmobile.utils;

import io.sentry.Sentry;
import okhttp3.OkHttpClient;
import okhttp3.Request;

public class LogReport {
//    Sentry.captureException(scope -> {
//        scope.setTag("custom_tag", "tag_value");
//        scope.setExtra("custom_key", "custom_value");
//    });

    // 创建 OkHttpClient 实例
//    OkHttpClient client = new OkHttpClient();

    // 创建请求
//   NetUtils.getNetUtils().request("/interface-server/agent/uploadRegisteDriverInfo", json,
}
